import { G2plotColumnDirective } from './g2plot-column.directive';

describe('G2plotColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotColumnDirective();
    expect(directive).toBeTruthy();
  });
});
