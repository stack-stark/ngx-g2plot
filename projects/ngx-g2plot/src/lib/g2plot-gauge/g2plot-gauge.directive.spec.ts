import { G2plotGaugeDirective } from './g2plot-gauge.directive';

describe('G2plotGaugeDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotGaugeDirective();
    expect(directive).toBeTruthy();
  });
});
