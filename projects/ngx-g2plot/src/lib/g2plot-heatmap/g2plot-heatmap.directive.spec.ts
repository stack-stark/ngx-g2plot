import { G2plotHeatmapDirective } from './g2plot-heatmap.directive';

describe('G2plotHeatmapDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotHeatmapDirective();
    expect(directive).toBeTruthy();
  });
});
