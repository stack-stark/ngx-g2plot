import { G2plotCalendarDirective } from './g2plot-calendar.directive';

describe('G2plotCalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotCalendarDirective();
    expect(directive).toBeTruthy();
  });
});
