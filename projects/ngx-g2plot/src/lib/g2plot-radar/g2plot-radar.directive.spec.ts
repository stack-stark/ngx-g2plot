import { G2plotRadarDirective } from './g2plot-radar.directive';

describe('G2plotRadarDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotRadarDirective();
    expect(directive).toBeTruthy();
  });
});
