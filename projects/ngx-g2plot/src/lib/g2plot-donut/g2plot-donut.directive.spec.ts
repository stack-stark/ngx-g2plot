import { G2plotDonutDirective } from './g2plot-donut.directive';

describe('G2plotDonutDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotDonutDirective();
    expect(directive).toBeTruthy();
  });
});
