import { G2plotStockDirective } from './g2plot-stock.directive';

describe('G2plotStockDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotStockDirective();
    expect(directive).toBeTruthy();
  });
});
