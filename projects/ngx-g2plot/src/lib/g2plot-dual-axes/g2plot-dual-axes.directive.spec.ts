import { G2plotDualAxesDirective } from './g2plot-dual-axes.directive';

describe('G2plotDualAxesDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotDualAxesDirective();
    expect(directive).toBeTruthy();
  });
});
