import { G2plotRangeColumnDirective } from './g2plot-range-column.directive';

describe('G2plotRangeColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotRangeColumnDirective();
    expect(directive).toBeTruthy();
  });
});
