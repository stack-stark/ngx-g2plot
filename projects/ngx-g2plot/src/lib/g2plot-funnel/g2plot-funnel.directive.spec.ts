import { G2plotFunnelDirective } from './g2plot-funnel.directive';

describe('G2plotFunnelDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotFunnelDirective();
    expect(directive).toBeTruthy();
  });
});
