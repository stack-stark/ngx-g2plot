import { G2plotLiquidDirective } from './g2plot-liquid.directive';

describe('G2plotLiquidDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotLiquidDirective();
    expect(directive).toBeTruthy();
  });
});
