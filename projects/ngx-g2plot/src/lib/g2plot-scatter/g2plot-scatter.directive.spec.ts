import { G2plotScatterDirective } from './g2plot-scatter.directive';

describe('G2plotScatterDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotScatterDirective();
    expect(directive).toBeTruthy();
  });
});
