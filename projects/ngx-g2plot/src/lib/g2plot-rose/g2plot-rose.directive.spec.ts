import { G2plotRoseDirective } from './g2plot-rose.directive';

describe('G2plotRoseDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotRoseDirective();
    expect(directive).toBeTruthy();
  });
});
