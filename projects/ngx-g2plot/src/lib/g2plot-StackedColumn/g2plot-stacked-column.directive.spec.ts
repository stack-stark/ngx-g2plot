import { G2plotStackedColumnDirective } from './g2plot-stacked-column.directive';

describe('G2plotStackedColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotStackedColumnDirective();
    expect(directive).toBeTruthy();
  });
});
