import { G2plotRangeBarDirective } from './g2plot-range-bar.directive';

describe('G2plotRangeBarDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotRangeBarDirective();
    expect(directive).toBeTruthy();
  });
});
