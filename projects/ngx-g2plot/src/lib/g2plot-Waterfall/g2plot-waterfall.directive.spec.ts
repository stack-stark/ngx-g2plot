import { G2plotWaterfallDirective } from './g2plot-waterfall.directive';

describe('G2plotWaterfallDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotWaterfallDirective();
    expect(directive).toBeTruthy();
  });
});
