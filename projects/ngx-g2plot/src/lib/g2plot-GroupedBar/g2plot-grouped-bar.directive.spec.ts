import { G2plotGroupedBarDirective } from './g2plot-grouped-bar.directive';

describe('G2plotGroupedBarDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotGroupedBarDirective();
    expect(directive).toBeTruthy();
  });
});
