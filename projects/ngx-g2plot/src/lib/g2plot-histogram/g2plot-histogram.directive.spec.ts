import { G2plotHistogramDirective } from './g2plot-histogram.directive';

describe('G2plotHistogramDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotHistogramDirective();
    expect(directive).toBeTruthy();
  });
});
