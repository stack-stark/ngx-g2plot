import { G2plotStackedAreaDirective } from './g2plot-stacked-area.directive';

describe('G2plotStackedAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotStackedAreaDirective();
    expect(directive).toBeTruthy();
  });
});
