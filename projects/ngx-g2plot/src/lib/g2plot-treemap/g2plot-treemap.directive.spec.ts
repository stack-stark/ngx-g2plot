import { G2plotTreemapDirective } from './g2plot-treemap.directive';

describe('G2plotTreemapDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotTreemapDirective();
    expect(directive).toBeTruthy();
  });
});
