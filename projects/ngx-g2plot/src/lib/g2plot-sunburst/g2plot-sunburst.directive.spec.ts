import { G2plotSunburstDirective } from './g2plot-sunburst.directive';

describe('G2plotSunburstDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotSunburstDirective();
    expect(directive).toBeTruthy();
  });
});
