import { G2plotWordClordDirective } from './g2plot-word-clord.directive';

describe('G2plotWordClordDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotWordClordDirective();
    expect(directive).toBeTruthy();
  });
});
