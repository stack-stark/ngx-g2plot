import { G2plotBulletDirective } from './g2plot-bullet.directive';

describe('G2plotBulletDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotBulletDirective();
    expect(directive).toBeTruthy();
  });
});
