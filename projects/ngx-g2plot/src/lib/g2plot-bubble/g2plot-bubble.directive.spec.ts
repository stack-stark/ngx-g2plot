import { G2plotBubbleDirective } from './g2plot-bubble.directive';

describe('G2plotBubbleDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotBubbleDirective();
    expect(directive).toBeTruthy();
  });
});
