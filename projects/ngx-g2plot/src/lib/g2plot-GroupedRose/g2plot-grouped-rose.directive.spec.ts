import { G2plotGroupedRoseDirective } from './g2plot-grouped-rose.directive';

describe('G2plotGroupedRoseDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotGroupedRoseDirective();
    expect(directive).toBeTruthy();
  });
});
