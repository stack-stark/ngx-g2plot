import { G2plotRadialBarDirective } from './g2plot-radial-bar.directive';

describe('G2plotRadialBarDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotRadialBarDirective();
    expect(directive).toBeTruthy();
  });
});
