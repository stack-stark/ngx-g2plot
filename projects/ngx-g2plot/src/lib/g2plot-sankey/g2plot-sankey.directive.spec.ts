import { G2plotSankeyDirective } from './g2plot-sankey.directive';

describe('G2plotSankeyDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotSankeyDirective();
    expect(directive).toBeTruthy();
  });
});
