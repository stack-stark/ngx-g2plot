import { G2plotAreaDirective } from './g2plot-area.directive';

describe('G2plotAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotAreaDirective();
    expect(directive).toBeTruthy();
  });
});
