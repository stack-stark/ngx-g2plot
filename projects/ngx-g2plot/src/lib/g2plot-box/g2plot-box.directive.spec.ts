import { G2plotBoxDirective } from './g2plot-box.directive';

describe('G2plotBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotBoxDirective();
    expect(directive).toBeTruthy();
  });
});
