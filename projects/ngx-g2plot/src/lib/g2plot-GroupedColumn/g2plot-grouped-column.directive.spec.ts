import { G2plotGroupedColumnDirective } from './g2plot-grouped-column.directive';

describe('G2plotGroupedColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotGroupedColumnDirective();
    expect(directive).toBeTruthy();
  });
});
