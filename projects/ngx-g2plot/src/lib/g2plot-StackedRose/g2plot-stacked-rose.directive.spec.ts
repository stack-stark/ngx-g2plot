import { G2plotStackedRoseDirective } from './g2plot-stacked-rose.directive';

describe('G2plotStackedRoseDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotStackedRoseDirective();
    expect(directive).toBeTruthy();
  });
});
