import { G2plotDensityHeatmapDirective } from './g2plot-density-heatmap.directive';

describe('G2plotDensityHeatmapDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotDensityHeatmapDirective();
    expect(directive).toBeTruthy();
  });
});
