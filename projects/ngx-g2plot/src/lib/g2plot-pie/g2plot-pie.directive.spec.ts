import { G2plotPieDirective } from './g2plot-pie.directive';

describe('G2plotPieDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotPieDirective();
    expect(directive).toBeTruthy();
  });
});
