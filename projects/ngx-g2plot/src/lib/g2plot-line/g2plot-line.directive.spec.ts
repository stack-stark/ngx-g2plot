import { G2plotLineDirective } from './g2plot-line.directive';

describe('G2plotLineDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotLineDirective();
    expect(directive).toBeTruthy();
  });
});
