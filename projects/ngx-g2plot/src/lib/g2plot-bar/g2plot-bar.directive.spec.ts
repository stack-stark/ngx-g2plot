import { G2plotBarDirective } from './g2plot-bar.directive';

describe('G2plotBarDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotBarDirective();
    expect(directive).toBeTruthy();
  });
});
