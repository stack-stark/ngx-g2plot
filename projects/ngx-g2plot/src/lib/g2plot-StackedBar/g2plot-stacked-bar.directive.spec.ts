import { G2plotStackedBarDirective } from './g2plot-stacked-bar.directive';

describe('G2plotStackedBarDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotStackedBarDirective();
    expect(directive).toBeTruthy();
  });
});
