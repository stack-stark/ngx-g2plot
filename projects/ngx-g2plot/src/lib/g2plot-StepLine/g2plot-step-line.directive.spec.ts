import { G2plotStepLineDirective } from './g2plot-step-line.directive';

describe('G2plotStepLineDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotStepLineDirective();
    expect(directive).toBeTruthy();
  });
});
