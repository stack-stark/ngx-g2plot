import { G2plotChordDirective } from './g2plot-chord.directive';

describe('G2plotChordDirective', () => {
  it('should create an instance', () => {
    const directive = new G2plotChordDirective();
    expect(directive).toBeTruthy();
  });
});
